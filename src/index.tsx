import React from 'react';
import {View} from 'react-native';
import {Button, RNText} from 'theme-component';

const Testing = () => {
  return (
    <View>
      <Button label="test" />
      <RNText label="test" />
    </View>
  );
};

export default Testing;
